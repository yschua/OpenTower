class = require 'libraries.middleclass.middleclass'
utils = require 'utils'
game = require 'game'

if love then
  loveframes = require 'libraries.LoveFrames.loveframes'
end

-- constants
c = {
  BLOCK_SIZE = 64
}
